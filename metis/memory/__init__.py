"""Memoria di Metis: cio' che resta fra un turno e l'altro.

In M4 contiene solo i comandi custom. In M5 si aggiunge la memoria
conversazionale, ed e' il motivo per cui il pacchetto nasce adesso invece di
lasciare i comandi dentro `metis/core/`.
"""
